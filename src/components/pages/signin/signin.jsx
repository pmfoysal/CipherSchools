import Logo from '@shared/logo';
import { SigninContainer, SigninContent } from './signin.styled';

export default function Signin() {
   return (
      <SigninContainer>
         <SigninContent>
            <Logo />
         </SigninContent>
      </SigninContainer>
   );
}
